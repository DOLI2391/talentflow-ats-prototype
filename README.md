# TalentFlow ATS Prototype

Prototipo navegable de ATS para gestión de vacantes, candidatos, Kanban, funnel de selección y autocompletado de CV/LinkedIn.

## Ejecutar localmente

```bash
npm install
npm start
```

Railway puede desplegarlo usando el script `start` de `package.json`.
